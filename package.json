{
  "name": "XMoto.js",
  "private": true,
  "devDependencies": {
    "coffeescript": "^1.12",
    "express": "^4.17.1"
  },
  "scripts": {
    "prettier-format-all": "yarn prettier --write src-ts",
    "build": "rm -r bin/* && yarn tsc"
  },
  "dependencies": {
    "prettier": "^2.8.8",
    "typescript": "^5.0.4"
  }
}
